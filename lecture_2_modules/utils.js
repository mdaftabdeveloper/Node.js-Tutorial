export const sum = (a, b) => a + b;
export const sub = (a, b) => a - b;
export const mul = (a, b) => a * b;
export const div = (a, b) => a / b;
// export these functions from utils.js to index.js using module.export
//module.exports = { sum, sub, mul, div };
// common js